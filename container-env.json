{
    "container": {
        "name": "hrueger",
        "scope": "adventskalender"
    },
    "variables": [
        {
            "name": "DB_NAME",
            "description": "The name of the database",
            "default": "db",
            "type": "string"
        },
        {
            "name": "DB_USER",
            "description": "The database user",
            "default": "root",
            "type": "string"
        },
        {
            "name": "DB_PASSWORD",
            "description": "The database password",
            "default": "",
            "type": "string"
        },
        {
            "name": "DB_PORT",
            "description": "The port of the database server",
            "default": 3306,
            "type": "number"
        },
        {
            "name": "DB_HOST",
            "description": "The database server hostname or ip address",
            "default": "localhost",
            "type": "string"
        },
        {
            "name": "JWT_SECRET",
            "description": "The JWT secret, just choose a random string",
            "default": "rwiuerslixalsdkxaxsdjxald",
            "type": "string"
        },
        {
            "name": "URL",
            "description": "The url where the Advent Calendar is available (no / at the end needed)",
            "default": "http://localhost:4200",
            "type": "string"
        },
        {
            "name": "MAIL_SERVER_HOST",
            "description": "The url of the SMTP to use",
            "default": "",
            "type": "string"
        },
        {
            "name": "MAIL_SERVER_PORT",
            "description": "The port of the SMTP to use",
            "default": "",
            "type": "number"
        },
        {
            "name": "MAIL_SERVER_USER",
            "description": "The username to use for the SMTP server",
            "default": "",
            "type": "string"
        },
        {
            "name": "MAIL_SERVER_PASSWORD",
            "description": "The password to use for the SMTP server",
            "default": "",
            "type": "string"
        },
        {
            "name": "MAIL_SENDER",
            "description": "The sender of the emails",
            "default": "no-reply@adventskalender",
            "type": "string"
        },
        {
            "name": "TEST_MODE",
            "description": "Whether to allow setting the current day with a cookie or not",
            "default": false,
            "type": "boolean"
        }
    ]
}